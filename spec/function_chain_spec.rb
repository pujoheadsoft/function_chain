require 'spec_helper'

describe FunctionChain do
  it 'has a version number' do
    expect(FunctionChain::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
